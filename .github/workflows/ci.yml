name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify skeleton
        run: |
          test -f README.md
          test -f SECURITY.md
          test -f docs/product-spec.md
          test -f docs/standards-spine.md
